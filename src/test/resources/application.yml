server.port: 8080

logging.level:
  root: ERROR
  com.cuupa.mailprocessor: DEBUG
  org.camunda: ERROR
  org.apache.ibatis: ERROR

mailprocessor:
  config: /opt/mailprocessor/config.json
  semanticUrl: "http://localhost:8081/api/rest/1.0/classifyText"

camunda:
  admin-user:
    id: demo
    password: demo
    firstName: John
    lastName: Doe
  filter.create: All tasks
  bpm.job-execution.enabled: true
